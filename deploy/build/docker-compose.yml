services:
  sous-marin-jaune:
    image: elyspio/coexya-sous-marin-jaune:2026.02.16.2
    build:
      context: ../..
      dockerfile: deploy/build/dockerfile
      args:
        - GITHUB_TOKEN=${GITHUB_TOKEN}
