services:
  sous-marin-jaune:
    image: elyspio/coexya-sous-marin-jaune:2026.02.25.1
    build:
      context: ../..
      dockerfile: deploy/build/dockerfile
      args:
        - GITHUB_TOKEN=${GITHUB_TOKEN}
